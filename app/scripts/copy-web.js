// Placeholder for the bundled web content step.
// Since we use server.url (live URL) in capacitor.config.json, the actual
// website is loaded from safwa-cemetery.com inside the webview. The www/
// folder only contains a minimal launcher that redirects to the live site
// in case the webview ever loses the server config.
console.log('Web content is loaded from server.url (live website). Nothing to copy.');
